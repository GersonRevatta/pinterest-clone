= form_for @pin do |f|
  - if @pin.errors.any?
    %h2= pluralize(@pin.errors.count, 'error')
    %ul
      - @pin.errors.full_messages.each do |msg|
        %li= msg
  = f.label :title
  = f.text_field :title
  %br/
  = f.label :description
  = f.text_area :description
  %br/
  = f.submit
